spring:
  datasource:
    url: jdbc:h2:file:${DB_PATH:~}/odc4.0;MODE=MySQL;NON_KEYWORDS=KEY,VALUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
jasypt:
  encryptor:
    bootstrap: false